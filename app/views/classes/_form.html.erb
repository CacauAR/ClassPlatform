<%= form_with(model: class, local: true) do |form| %>
  <% if class.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(class.errors.count, "error") %> prohibited this class from being saved:</h2>

      <ul>
      <% class.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :course_id %>
    <%= form.text_field :course_id %>
  </div>

  <div class="field">
    <%= form.label :professor_id %>
    <%= form.text_field :professor_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
